const MAP_WIDTH = 6000;
const MAP_HEIGHT = 6000;

const isSafari = /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
const isBrowserSupported = !isSafari;
//let randomX = random(0, MAP_WIDTH);
//let randomY = random(0, MAP_HEIGHT);
